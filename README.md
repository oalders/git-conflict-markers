# What is This?

A demo repo to explore how to handle git conflict markers.

## Set Up Git Hooks

```bash
./git/setup.sh
```

## Create a Merge Conflict:

```
git co -b conflict
git merge one
git merge two
```
